(function($){

    $.fn.scrolltop = function(params){

        var defaults = {};

        params = $.extend({}, defaults, params);

        return this.each(function(){

            var $element = $(this);

        });

    };

})(jQuery);
